import { COLORS } from './colors';
import { FONTS, FONT_WEIGHTS, FONT_SIZES } from './fonts';
import { SIZES } from './sizes';
import { SPACE, LINE_HEIGHTS } from './spacing';

const THEME = {
    COLORS,
    SPACE,
    LINE_HEIGHTS,
    SIZES,
    FONTS,
    FONT_SIZES,
    FONT_WEIGHTS,
};

export { THEME };
